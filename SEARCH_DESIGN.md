# 검색 기능 설계 문서

## 배경

기존 검색은 입력어를 12개 마크다운 파일에 순차적으로 fetch해 첫 번째 매칭 섹션만 로드하는 방식이었다.
핵심 문제는 세 가지였다.

1. **단일 결과** — 첫 번째 매칭 섹션에서 즉시 중단, 다른 섹션의 관련 내용은 표시하지 않음
2. **미리보기 없음** — 섹션을 직접 로드해봐야 내용 확인 가능
3. **느린 응답** — 매 검색마다 최대 12회 network fetch 발생

## 아이디어: 색인표 우선 검색

책 뒤의 색인(index)처럼 사전에 만들어둔 색인표를 먼저 검색하고,
결과 목록에서 항목을 선택해 해당 페이지로 이동하는 구조.

```
사용자 입력
  │
  ▼
search-index.json 조회  ←── 사전 생성된 670개 항목
  │
  ├─ 결과 있음 → 드롭다운 표시 → 클릭 → 섹션 로드 + 하이라이트
  │
  └─ 결과 없음 → 전문 검색 폴백 (기존 fetch 순회 방식)
```

## 구성 요소

### 1. 색인 생성 스크립트 (`generate-index.js`)

마크다운 파일을 파싱해 `search-index.json`을 생성하는 Node.js 스크립트.
빌드 도구 없이 `node generate-index.js` 한 줄로 실행.

**추출 대상 3가지:**

| 타입 | 마크다운 패턴 | 예시 |
|------|--------------|------|
| `heading` | `# ~ ####` 헤딩 | `## 감리 핵심 원칙` |
| `keyword` | `**굵은 텍스트**` | `**독립성**: 감리자는 ...` |
| `keyword` | `- 항목: 설명` 리스트 | `- MSA 적정성: 서비스 간 ...` |

**색인 항목 구조:**

```json
{
  "term":    "MSA(마이크로서비스 아키텍처) 적정성",
  "section": "latest_checkpoints",
  "label":   "최신 감리 포인트",
  "type":    "keyword",
  "context": "최신 사업별 감리 점검 포인트",
  "preview": "서비스 간 결합도를 낮추고 독립적인 배포가 가능한 구조인지 점검..."
}
```

- `section`: `loadMarkdownContent()`에 전달하는 파일 ID
- `label`: 드롭다운에 표시하는 섹션 한글명
- `context`: 상위 헤딩 (계층 위치 파악용)
- `preview`: 해당 위치 직후 텍스트 발췌 (최대 90자)

**중복 제거 기준:**
- `heading`: `(term + section)` 조합
- `keyword`: `(term + section + context)` 조합

**현재 색인 규모:** 670개 항목 (헤딩 232개, 키워드 438개)

### 2. 색인 검색 (`script.js`)

```
loadSearchIndex()   — 최초 1회 fetch 후 메모리 캐시
queryIndex()        — term / context / preview 3개 필드 부분일치 검색
renderResults()     — 드롭다운 HTML 렌더링, 검색어 <mark> 강조
```

**실시간 입력 처리:**
- `input` 이벤트 + 300ms 디바운스 → 색인 조회 → 드롭다운 표시
- `Enter` / 검색 버튼 → 색인 없으면 전문 검색 폴백 실행
- `Escape` / 바깥 클릭 → 드롭다운 닫기

### 3. 결과 드롭다운 UI (`styles.css`, `index.html`)

검색창 바로 아래 `position: absolute`로 오버레이.

```
┌──────────────────────────────────────────┐
│ 5개 항목 발견                             │  ← 결과 수
├──────────────────────────────────────────┤
│ MSA(마이크로서비스 아키텍처) 적정성        │  ← term (검색어 하이라이트)
│ [최신 감리 포인트] 최신 사업별 감리 포인트 │  ← label + context
│ 서비스 간 결합도를 낮추고 독립적인 배포... │  ← preview
├──────────────────────────────────────────┤
│ ...                                       │
└──────────────────────────────────────────┘
```

## 색인 업데이트

### 기존 파일 내용 변경

`content/*.md` 내용을 수정한 경우 스크립트를 재실행하면 된다.

```bash
node generate-index.js
```

### 새 마크다운 파일 추가

새 파일을 추가할 때는 스크립트와 앱 코드를 함께 수정해야 한다.

**1단계 — `generate-index.js` 상단 `SECTION_LABELS` 객체에 항목 추가**

```js
const SECTION_LABELS = {
    // ... 기존 항목 ...
    new_section: '새 섹션 이름',   // ← 추가
};
```

`SECTION_LABELS`의 키가 `content/` 디렉터리 내 파일명(`.md` 제외)과 일치해야 한다.

**2단계 — 색인 재생성**

```bash
node generate-index.js
```

**3단계 — `script.js` 내비게이션 연결**

새 섹션이 네비게이션 메뉴나 내부 링크로 접근 가능해야 한다면
`loadMarkdownContent()` 의 분기 처리 및 HTML 메뉴에도 추가한다.

**4단계 — 전문 검색 폴백 목록 추가 (선택)**

색인 미매칭 시 폴백하는 `performSearch()` 의 `sections` 배열에도 추가한다.

```js
const sections = [
    'introduction', 'preparation', /* ... */ 'new_section'  // ← 추가
];
```

### 체크리스트 요약

| 작업 | 파일 |
|------|------|
| `SECTION_LABELS`에 섹션 ID·이름 등록 | `generate-index.js` |
| 색인 재생성 (`node generate-index.js`) | `search-index.json` |
| 내비게이션 메뉴 링크 추가 | `index.html` |
| 폴백 검색 대상 목록 추가 | `script.js` |

## 한계 및 개선 방향

| 한계 | 개선 방향 |
|------|----------|
| 색인에 없는 표현은 폴백에 의존 | 형태소 분석 기반 키워드 추출 적용 |
| 동의어 처리 없음 | 동의어 사전 JSON 별도 관리 |
| 섹션 내 위치(앵커) 정보 없음 | 헤딩별 anchor ID 생성 및 스크롤 이동 |
| 콘텐츠 변경 시 수동 재생성 필요 | git hook(`pre-commit`)으로 자동화 |
